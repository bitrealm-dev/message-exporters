[package]
name = "message-sbr"
version = "0.1.0"
edition = "2024"
description = "SMS Backup & Restore (SyncTech) XML codec"
license = "MIT OR Apache-2.0"

[dependencies]
anyhow = "1.0.103"
base64 = "0.22.1"

[dev-dependencies]
tempfile = "3.27.0"
